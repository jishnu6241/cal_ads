import 'package:flutter/material.dart';

class TaxiTopMedia extends StatelessWidget {
  const TaxiTopMedia({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Taxi Top Media"),
      ),
    );
  }
}
