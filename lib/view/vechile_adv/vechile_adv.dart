import 'package:flutter/material.dart';

class VechileAdv extends StatelessWidget {
  const VechileAdv({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("VechileAdv"),
      ),
    );
  }
}
